import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.caviarscout.app',
  appName: 'CaviarScout',
  webDir: 'dist/sakai-ng/browser'
};

export default config;
