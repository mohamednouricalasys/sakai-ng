interface Window {
    env: {
        keycloakclientId: string;
        keycloakclientsecret: string;
        keycloakRealm: string;
        keycloakUrl: string;
        baseFrontUrl: string;
        apiUrl: string;
        allowedUrls: string[];
        production: boolean;
    };
}
